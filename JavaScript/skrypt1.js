const day = 123;
            const hours = day * 24;
            const minutes = hours * 60;
            const seconds = minutes * 60;

            const textToDisplay = "Od początku roku minęło " + day + "dni. To " + hours + " godzin, " 
            + minutes + " minut lub " + seconds + " sekund."

            alert(textToDisplay);